{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "project for storing postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests": "newman run tests/Jenkins_Collection.postman_collection.json"
  },
  "author": "Thangam",
  "dependencies": {
    "newman": "^4.1.0"
  }
}
